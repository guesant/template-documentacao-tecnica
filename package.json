{
  "name": "template-documentacao-tecnica",
  "private": true,
  "license": "Unlicense",
  "description": "Quality gates de documentação (lint, ortografia, tipografia) para este template",
  "scripts": {
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "lint:spell": "cspell --no-progress --show-context \"**/*.md\"",
    "lint:tipografia": "python3 scripts/check_tipografia.py",
    "lint": "npm run lint:md && npm run lint:spell && npm run lint:tipografia"
  },
  "devDependencies": {
    "@cspell/dict-pt-br": "^2.4.2",
    "cspell": "^10.1.0",
    "markdownlint-cli2": "^0.23.2"
  }
}
